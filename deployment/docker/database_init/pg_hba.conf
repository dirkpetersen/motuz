# TYPE  DATABASE        USER            ADDRESS                 METHOD
# Only allow password (md5) connections from this host; the containers use host networking
host    all             all             samehost                md5
